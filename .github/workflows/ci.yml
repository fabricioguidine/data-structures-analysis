name: CI

on:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build & Test (${{ matrix.os }}, JDK ${{ matrix.java-version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        java-version: ['17', '21']
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK ${{ matrix.java-version }}
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ matrix.java-version }}
          cache: maven

      - name: Show versions
        shell: bash
        run: java -version

      - name: Build and test (Unix)
        if: runner.os != 'Windows'
        run: ./mvnw -B test

      - name: Build and test (Windows)
        if: runner.os == 'Windows'
        run: ./mvnw.cmd -B test

  e2e:
    name: End-to-end & coverage (ubuntu, JDK 21)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      # Runs Surefire (unit) + Failsafe (*IntegrationTest end-to-end) + JaCoCo.
      # Lint is enforced separately in the `lint` job below.
      - name: Verify with coverage
        run: ./mvnw -B verify -Dspotless.check.skip=true -Dcheckstyle.skip=true

      - name: Upload JaCoCo report
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: target/site/jacoco/

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: target/site/jacoco/jacoco.xml
          fail_ci_if_error: false

  lint:
    name: Lint (Spotless + Checkstyle, informational)
    runs-on: ubuntu-latest
    # Pre-existing formatting debt is reported but does not gate the build.
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Spotless check
        run: ./mvnw -B spotless:check

      - name: Checkstyle
        run: ./mvnw -B checkstyle:check
